// announcements.tsx

import Layout from "@/templates/Layout";
import Messages from "@/views/Messages";

const Announcements = () => {
  return (
    <Layout>
      <h1>Intro</h1>
      <Messages></Messages>
    </Layout>
  )
};

export default Announcements;

